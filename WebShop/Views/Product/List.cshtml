@using WebShop.Bo
@model WebShop.Web.ViewModels.ProductListViewModel

@{
    ViewData["Title"] = "List";
}


@*@await Html.PartialAsync("Carousel")*@

@{
    foreach (Product product in Model.Products)
    {
        @await Html.PartialAsync("ProductsSummary", product)
    }
}

